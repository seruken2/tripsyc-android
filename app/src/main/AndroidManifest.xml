<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TripwaveApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tripwave"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Tripwave">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Join link: tripwave://join/{tripId}?code=X
                 No autoVerify — Android App Links auto-verify only
                 applies to http/https schemes via Digital Asset Links;
                 a tripwave:// custom scheme is always handled by the
                 app since nobody else registered it. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tripwave" android:host="join" />
            </intent-filter>

            <!-- Universal links -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="www.tripwave.co" />
            </intent-filter>

            <!-- Google Sign-In callback: tripwave://google-auth?session=... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tripwave" android:host="google-auth" />
            </intent-filter>
        </activity>

        <!-- FCM Service -->
        <service
            android:name=".push.TripFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Invite Accept / Decline broadcast receiver for notification
             action buttons. -->
        <receiver
            android:name=".push.InviteActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.tripwave.app.INVITE_ACCEPT" />
                <action android:name="com.tripwave.app.INVITE_DECLINE" />
            </intent-filter>
        </receiver>

        <!-- Chat inline reply receiver. -->
        <receiver
            android:name=".push.ChatReplyReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.tripwave.app.CHAT_REPLY" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing the Wrapped card image via
             Intent.ACTION_SEND. Scopes to cache/wrapped only. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/share_paths" />
        </provider>

        <!-- Home-screen widget: shows the user's next upcoming trip
             with a live countdown. Backed by Glance — see
             com.tripwave.app.widget.NextTripWidget for the composable
             and NextTripWidgetUpdater for the in-app writer. -->
        <receiver
            android:name=".widget.NextTripWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_next_trip_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_trip_widget_info" />
        </receiver>

    </application>

</manifest>
